rules:
  - id: aulapay-sql-injection
    message: Posible SQL Injection. studentId es concatenado directamente en la consulta SQL.
    severity: ERROR
    languages:
      - javascript
    pattern-regex: 'student_id\s*=\s*"\s*\+\s*studentId'