on:
  push:
    branches:
      - main
      - vulnerable-demo

  pull_request:
    branches:
      - main


  deploy-simulation:
    name: "CD - Preproduccion simulada"
    runs-on: ubuntu-latest

    needs:
      - security-gate

    steps:
      - name: Autorizar despliegue
        run: |
          echo "======================================"
          echo "      FASE CD - PREPRODUCCION"
          echo "======================================"
          echo ""
          echo "Todos los controles CI fueron aprobados."
          echo "Security Gate: APROBADO"
          echo ""
          echo "DESPLIEGUE A PREPRODUCCION AUTORIZADO"
